version: '3'
services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  web:
    image: bsb99/living-by-myself-server:latest
    ports:
      - "8080:8080"
    depends_on:
      - redis
    networks:
      - my_network

networks:
  my_network:
